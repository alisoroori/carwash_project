@echo off
echo Tailwind build helper (disabled) - project now uses Tailwind CDN for convenience.
echo For production builds, restore or re-create a build step that compiles Tailwind via the CLI or PostCSS,
echo and include the resulting compiled CSS file in your HTML (recommended for purging unused CSS).
echo.
echo To re-enable local build, edit this file and add your tailwindcss.exe commands back.
pause