<?php
// In any PHP file
require_once 'backend/includes/config.php';

$dbHost = Config::get('DB_HOST');
$isProduction = Config::isProduction();

if (Config::isDevelopment()) {
    error_reporting(E_ALL);
    ini_set('display_errors', 1);
}

// In API endpoints
if (Config::isProduction()) {
    header('Access-Control-Allow-Origin: ' . Config::get('APP_URL'));
} else {
    header('Access-Control-Allow-Origin: *');
}

// Database Configuration
DB_HOST=localhost
DB_NAME=carwash
DB_USER=root
DB_PASS=

// Application Configuration
APP_NAME="Car Wash Management System"
APP_URL=http://localhost/carwash_project
APP_DEBUG=true
APP_TIMEZONE=Asia/Tehran
APP_ENV=development

// Security
SESSION_LIFETIME=1800
APP_KEY=

// Upload Directories
UPLOAD_DIR=uploads
PROFILE_UPLOAD_DIR=backend/auth/uploads/profiles

# Test Database (used by automated tests / CI)
# If left empty, test scripts will fall back to the primary DB_* values above.
# Recommended to use a dedicated test database to avoid clobbering production/dev data.
DB_TEST_HOST=127.0.0.1
DB_TEST_NAME=carwash_test_db
DB_TEST_USER=root
DB_TEST_PASS=