/* Local stub for Alpine.js
   This is a tiny placeholder that provides the minimal `Alpine` global so
   templates that reference Alpine won't throw if the real library isn't present.
   Replace this with the official Alpine.js build when you want full functionality.
*/
window.Alpine = window.Alpine || {};
window.Alpine.start = window.Alpine.start || function(){ console.info('Alpine stub started'); };
(function(){ if(!window.Alpine || !window.Alpine.start){ console.info('Running Alpine stub'); } })();
